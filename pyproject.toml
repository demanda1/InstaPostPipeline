[project]
name = "instapost-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "python-dotenv",
    "requests",
    "huggingface-hub",
    "pillow",
    "httpcore",
    "httpx"
]
